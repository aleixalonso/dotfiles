## Git commits

Follow the Conventional Commits format: `type(scope): description`. The `scope` is optional but preferred when it adds clarity.

When creating git commits, use a single-line `-m` message only. Do not include a description body.
